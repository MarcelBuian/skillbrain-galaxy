// Atribuire conditionata

// <expresie booleana> ? valoare1 : valoare2;

const a = -6;

const b = a > 0 ? a : -a;


// const c = (a > 0 ? a : -a);
//   (a > 0)   ?   (a)   :   (-a)

console.log("b = ", b);